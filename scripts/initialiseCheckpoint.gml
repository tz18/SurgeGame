point2 = instance_nearest(x, y, CheckpointB); //Finds where the nearest Checkpoint B/blue checkpoint is.
finishLine = false; //By default, a checkpoint is not the finish line. However, a finish line must be defined if the course is not to break after a single circuit.
ship = 0;
